# Product Service

## Purpose
Handles product APIs.

## Running Locally
1. Copy `.env.example` to `.env.local`
2. Run `npm install` from the monorepo root
3. Start with `npm run start --workspace=product-service`
