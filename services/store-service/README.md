# Store Service

## Purpose
Handles store APIs.

## Running Locally
1. Copy `.env.example` to `.env.local`
2. Run `npm install` from the monorepo root
3. Start with `npm run start --workspace=store-service`
